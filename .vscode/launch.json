{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run timedisplay",
      "type": "debugpy",
      "request": "launch",
      "module": "timedisplay.main",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  ]
}